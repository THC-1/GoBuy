系统基础与权限模块（后台）
后台管理员登录： 包含账号密码验证、验证码生成与校验、JWT Token 下发。

员工/用户管理： 维护后台的账号信息，支持重置密码、禁用账号等。

角色管理 (RBAC核心)： 创建不同的角色（如超级管理员、运营专员、客服），并为角色分配不同的权限。

菜单/权限管理： 动态控制后台左侧的导航菜单。比如“客服”登录后，只能看到“订单管理”，看不到“系统设置”。

数据字典管理： 统一管理系统里的下拉框选项。比如将“订单状态（待支付、已发货等）”存在字典表里，方便后续统一修改，而不是写死在代码里。
  
商品中心模块（前后台联动）
商品分类管理： 实现至少三级的分类树（例如：数码 -> 手机 -> 游戏手机）。

商品属性/规格模板： 极其重要。比如为“手机”类目提前定义好需要填写“屏幕尺寸”、“内存”、“颜色”等规格。

商品 SPU 管理（标准产品）： 录入商品的通用信息，如商品名称、副标题、商品主图、富文本详情介绍。

商品 SKU 管理（库存量单位）： 这是难点。同一个手机，不同颜色和内存的组合，其价格和库存是不同的。这就需要在发品时动态生成 SKU 列表。

商品上下架管理： 控制商品在前端的展示状态。

C端会员/用户模块（前端）
C端用户注册/登录： 面向普通买家的账号系统。

个人信息维护： 修改头像、昵称、性别等基础资料。

收货地址管理： 增、删、改、查收货地址，并支持设置“默认地址”。

我的收藏夹： 用户可以将心仪的商品加入收藏列表。

商品评价体系： 交易完成后，用户可以对商品打分并留下文字评价。


  物购车与订单流转（前后台联动）
  购物车管理：

添加商品到购物车（需精确到具体 SKU）。

修改购物车商品数量。

勾选准备结算的商品，实时计算总金额。

订单生成与拆解：

在结算页选择地址。

提交订单（难点：需要考虑并发问题，比如超卖，这里初期可以用简单的数据库乐观锁解决）。

订单状态机（状态流转）：

待支付： 生成订单但未付款，支持超时自动取消。

待发货（已支付）： 后台管理员看到订单，录入快递单号。

待收货（已发货）： 用户在前端确认收货。

已完成/已取消： 订单终态。

模拟支付通道： 不需要真的接微信/支付宝，做一个模拟的支付接口（点击“确认支付”后，后端直接将订单状态改为“已支付”，并记录一条流水）。
