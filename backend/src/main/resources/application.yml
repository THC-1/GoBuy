server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gobuy?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      logic-delete-field: deleted
      id-type: assign_id

jwt:
  secret: LuminaDigitalMall2024SecretKeyForJwtTokenGenerationAndValidation
  access-token-expiration: 7200000
  refresh-token-expiration: 604800000
