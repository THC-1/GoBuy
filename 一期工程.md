# Lumina 数码商城 — 一期工程规划文档

> **版本**: v1.0\
> **更新日期**: 2026-01-05\
> **项目代号**: GoBuy Phase 1\
> **核心目标**: 搭建C端基础框架，实现用户体系与商品浏览的核心链路

***

## 一、工程概述

### 1.1 项目定位

Lumina 是一个高端数码产品电商平台，一期工程聚焦于**C端用户的最小可用产品（MVP）**，建立用户认证体系与商品浏览体验的基础架构。

### 1.2 技术选型确认

| 层级          | 技术                    | 版本      | 说明                                 |
| ----------- | --------------------- | ------- | ---------------------------------- |
| **后端框架**    | Spring Boot           | 3.5.13  | Java 17                            |
| **ORM框架**   | MyBatis-Plus          | 3.5.x   | 简化CRUD操作                           |
| **数据库**     | MySQL                 | 8.0+    | 关系型数据库                             |
| **认证方案**    | JWT + Spring Security | -       | <br />                             |
| **前端框架**    | Vue 3                 | ^3.5.31 | Composition API + `<script setup>` |
| **构建工具**    | Vite                  | ^8.0.3  | 极速开发体验                             |
| **UI组件库**   | Element Plus          | ^2.13.6 | 基础组件（深度定制）                         |
| **样式方案**    | Tailwind CSS          | ^4.2.2  | 原子化CSS                             |
| **状态管理**    | Pinia                 | ^3.0.4  | 轻量级状态管理                            |
| **HTTP客户端** | Axios                 | ^1.x    | 请求封装                               |

### 1.3 一期范围边界

```
✅ 一期内完成:
├── 用户模块（注册/登录/Token管理）
├── 首页（Hero + 商品展示 + 服务特色）
└── 商品详情页（信息展示 + SKU选择）

❌ 一期不包含:
├── 购物车功能
├── 订单系统
├── 支付集成
├── 后台管理系统
├── 商品搜索/筛选
└── 用户中心（除登录态外的个人信息管理）
```

***

## 二、模块一：C端用户登录注册

### 2.1 功能需求清单

| 编号   | 功能点       | 优先级 | 说明                                 |
| ---- | --------- | --- | ---------------------------------- |
| U-01 | 用户注册      | P0  | 用户名 + 邮箱 + 密码 注册                   |
| U-02 | 用户登录      | P0  | 邮箱/用户名 + 密码 登录                     |
| U-03 | 登录状态持久化   | P0  | JWT Token + Refresh Token 双Token机制 |
| U-04 | Token自动续期 | P1  | Access Token过期时使用Refresh Token刷新   |
| U-05 | 路由守卫      | P0  | 未登录用户访问需登录页面时重定向至登录页               |
| U-06 | 登录状态全局感知  | P0  | Header区域显示用户头像或登录按钮                |
| U-07 | 表单实时校验    | P0  | 邮箱格式、密码强度、用户名规则                    |
| U-08 | 退出登录      | P0  | 清除Token，跳转首页                       |

### 2.2 数据库设计

#### 表: `user`（用户表）

```sql
CREATE TABLE `user` (
    `id`              BIGINT          NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `username`        VARCHAR(50)     NOT NULL                COMMENT '用户名',
    `email`           VARCHAR(100)    NOT NULL                COMMENT '邮箱',
    `password`        VARCHAR(255)    NOT NULL                COMMENT '密码(BCrypt加密)',
    `nickname`        VARCHAR(50)     DEFAULT NULL            COMMENT '昵称',
    `avatar_url`      VARCHAR(500)    DEFAULT NULL            COMMENT '头像URL',
    `status`          TINYINT         NOT NULL DEFAULT 1      COMMENT '状态: 0-禁用 1-正常',
    `last_login_time` DATETIME        DEFAULT NULL            COMMENT '最后登录时间',
    `created_at`      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at`      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted`         TINYINT         NOT NULL DEFAULT 0      COMMENT '逻辑删除: 0-未删除 1-已删除',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    UNIQUE KEY `uk_email` (`email`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

#### 字段约束说明

| 字段       | 约束规则                 | 校验说明                |
| -------- | -------------------- | ------------------- |
| username | 2-20字符，字母数字下划线，不能纯数字 | 前后端双重校验             |
| email    | 合法邮箱格式               | 前后端双重校验             |
| password | 6-20字符，必须包含字母和数字     | 前端提示 + 后端BCrypt加密存储 |
| nickname | 可选，2-20字符            | 注册时可留空，默认取username  |

### 2.3 后端API设计

#### 2.3.1 接口总览

| 方法   | 路径                           | 功能       | 认证               |
| ---- | ---------------------------- | -------- | ---------------- |
| POST | `/api/v1/auth/register`      | 用户注册     | 否                |
| POST | `/api/v1/auth/login`         | 用户登录     | 否                |
| POST | `/api/v1/auth/refresh-token` | 刷新Token  | 否（Refresh Token） |
| GET  | `/api/v1/users/me`           | 获取当前用户信息 | 是                |
| POST | `/api/v1/auth/logout`        | 退出登录     | 是                |

#### 2.3.2 DTO定义

**RegisterRequest（注册请求）**

```java
public class RegisterRequest {
    @NotBlank(message = "用户名不能为空")
    @Size(min = 2, max = 20, message = "用户名长度为2-20个字符")
    @Pattern(regexp = "^[a-zA-Z0-9_]+$", message = "用户名只能包含字母、数字、下划线")
    private String username;

    @NotBlank(message = "邮箱不能为空")
    @Email(message = "邮箱格式不正确")
    private String email;

    @NotBlank(message = "密码不能为空")
    @Size(min = 6, max = 20, message = "密码长度为6-20个字符")
    @Pattern(regexp = "^(?=.*[a-zA-Z])(?=.*\\d).+$", message = "密码必须包含字母和数字")
    private String password;
}
```

**LoginRequest（登录请求）**

```java
public class LoginRequest {
    @NotBlank(message = "账号不能为空")
    private String account;  // 支持邮箱或用户名

    @NotBlank(message = "密码不能为空")
    private String password;
}
```

**LoginVO（登录响应）**

```java
public class LoginVO {
    private String accessToken;       // Access Token (有效期2小时)
    private String refreshToken;      // Refresh Token (有效期7天)
    private Long expiresIn;           // Access Token过期时间(秒)
    private UserVO user;              // 用户基本信息
}
```

**UserVO（用户信息响应）**

```java
public class UserVO {
    private Long id;
    private String username;
    private String email;
    private String nickname;
    private String avatarUrl;
}
```

#### 2.3.3 接口详细规范

**POST /api/v1/auth/register — 用户注册**

- **请求体**: `RegisterRequest`
- **成功响应** (201):

```json
{
    "code": 201,
    "message": "注册成功",
    "data": {
        "accessToken": "eyJhbGciOiJIUzI1NiJ9...",
        "refreshToken": "eyJhbGciOiJIUzI1NiJ9...",
        "expiresIn": 7200,
        "user": {
            "id": 1,
            "username": "lumina_user",
            "email": "user@lumina.com",
            "nickname": "lumina_user",
            "avatarUrl": null
        }
    }
}
```

- **错误响应**:
  - `400`: 参数校验失败（返回具体字段错误信息）
  - `409`: 用户名或邮箱已存在
  - `500`: 服务器内部错误

**POST /api/v1/auth/login — 用户登录**

- **请求体**: `LoginRequest`
- **成功响应** (200): 同注册响应结构
- **错误响应**:
  - `400`: 参数校验失败
  - `401`: 账号或密码错误
  - `403`: 账号已被禁用

**POST /api/v1/auth/refresh-token — 刷新Token**

- **请求头**: `X-Refresh-Token: <refresh_token>`
- **成功响应** (200): 返回新的 accessToken + refreshToken 对
- **错误响应**:
  - `401`: Refresh Token无效或已过期

**GET /api/v1/users/me — 获取当前用户信息**

- **请求头**: `Authorization: Bearer <access_token>`
- **成功响应** (200):

```json
{
    "code": 200,
    "message": "success",
    "data": {
        "id": 1,
        "username": "lumina_user",
        "email": "user@lumina.com",
        "nickname": "数码爱好者",
        "avatarUrl": "https://cdn.lumina.com/avatar/default.png"
    }
}
```

### 2.4 后端代码结构

```
backend/src/main/java/com/example/backend/
├── config/
│   ├── SecurityConfig.java              # Spring Security配置
│   ├── JwtConfig.java                   # JWT配置属性
│   ├── MybatisPlusConfig.java           # MyBatis-Plus配置
│   └── WebConfig.java                   # CORS跨域配置
├── controller/
│   └── AuthController.java              # 认证相关接口
├── service/
│   ├── AuthService.java                 # 认证业务接口
│   └── impl/AuthServiceImpl.java        # 认证业务实现
├── entity/
│   └── User.java                        # 用户实体类
├── mapper/
│   └── UserMapper.java                  # 用户Mapper接口
├── dto/
│   ├── request/
│   │   ├── LoginRequest.java            # 登录请求DTO
│   │   └── RegisterRequest.java         # 注册请求DTO
│   └── response/
│       ├── LoginVO.java                 # 登录响应VO
│       └── UserVO.java                  # 用户信息VO
├── common/
│   ├── Result.java                      # 统一响应包装类
│   ├── ResultCode.java                  # 响应状态码枚举
│   └── exception/
│       ├── GlobalExceptionHandler.java  # 全局异常处理
│       └── BusinessException.java      # 业务异常类
└── util/
    ├── JwtUtil.java                     # JWT工具类
    └── RedisUtil.java                   # Redis工具类
```

### 2.5 前端实现规范

#### 2.5.1 页面路由

| 路径       | 组件              | 说明                |
| -------- | --------------- | ----------------- |
| `/login` | `LoginView.vue` | 登录/注册一体化页面（Tab切换） |

#### 2.5.2 核心组件

**AuthForm.vue — 认证表单组件**

功能要点:

- 登录/注册模式切换（同一个组件，通过 prop 控制）
- 表单字段:
  - **登录模式**: account（账号）+ password（密码）+ rememberMe（记住我）
  - **注册模式**: username（用户名）+ email（邮箱）+ password（密码）+ confirmPassword（确认密码）
- 实时校验（Element Plus Form Rules）
- 密码可见性切换
- 提交按钮 loading 状态
- 错误消息 Toast 提示

**校验规则明细**:

| 字段              | 规则                  | 触发时机          | 错误提示              |
| --------------- | ------------------- | ------------- | ----------------- |
| username        | 必填, 2-20字符, 字母数字下划线 | blur + change | 请输入用户名 / 用户名格式不正确 |
| email           | 必填, 邮箱格式            | blur + change | 请输入邮箱 / 邮箱格式不正确   |
| password        | 必填, 6-20字符, 含字母+数字  | blur + change | 请输入密码 / 密码强度不足    |
| confirmPassword | 必填, 与password一致     | blur + change | 请确认密码 / 两次密码不一致   |
| account         | 必填                  | blur + change | 请输入账号             |

#### 2.5.3 状态管理（Pinia Store）

**useUserStore.ts**

```typescript
// Store 结构
interface UserState {
  token: string | null           // Access Token
  refreshToken: string | null    // Refresh Token
  userInfo: UserVO | null        // 用户信息
  isLoggedIn: boolean            // 登录状态标识
}

// Actions
- login(credentials: LoginParams)      // 登录
- register(data: RegisterParams)       // 注册
- logout()                             // 退出登录
- fetchUserInfo()                      // 获取用户信息
- refreshToken()                       // 刷新Token
- loadFromStorage()                    // 从localStorage恢复状态
```

**Token存储策略**:

| 数据           | 存储位置                              | 说明           |
| ------------ | --------------------------------- | ------------ |
| accessToken  | 内存（Pinia State）+ localStorage备份   | 敏感信息，内存优先    |
| refreshToken | httpOnly Cookie（推荐）或 localStorage | 安全性优先用Cookie |
| userInfo     | localStorage                      | 用户基本信息公开存储   |

#### 2.5.4 HTTP请求拦截器

**Axios实例配置**:

```typescript
// 请求拦截器
- 注入 Authorization: Bearer <token>
- 注入 Content-Type: application/json

// 响应拦截器
- 401 错误处理:
  - 尝试使用 refreshToken 刷新
  - 刷新成功 → 重试原请求
  - 刷新失败 → 清除登录态，跳转登录页
- 统一错误提示（ElMessage）
- 响应数据解包（提取 data 字段）
```

#### 2.5.5 路由守卫

```typescript
// router/index.ts 配置
const routes = [
  {
    path: '/login',
    component: LoginView,
    meta: { requiresAuth: false }  // 公开页面
  },
  {
    path: '/',
    component: ClientLayout,
    meta: { requiresAuth: false },  // 首页允许未登录访问
    children: [
      { path: '', component: HomeView },
      { path: 'product/:id', component: ProductDetailView }
    ]
  }
]

// 全局前置守卫逻辑
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  
  // 目标路由需要登录 且 当前未登录
  if (to.meta.requiresAuth && !userStore.isLoggedIn) {
    next({ path: '/login', query: { redirect: to.fullPath } })
    return
  }
  
  // 已登录用户访问登录页 → 重定向首页
  if (to.path === '/login' && userStore.isLoggedIn) {
    next({ path: '/' })
    return
  }
  
  next()
})
```

### 2.6 验收标准

- [ ] 用户可使用邮箱+用户名+密码完成注册
- [ ] 注册时重复用户名/邮箱返回明确错误提示
- [ ] 用户可使用邮箱或用户名+密码登录
- [ ] 登录成功后Header区域显示用户头像
- [ ] Token过期前自动续期，用户无感知
- [ ] 页面刷新后登录态保持
- [ ] 未登录访问受保护路径自动跳转登录页
- [ ] 退出登录后清除所有状态并跳转首页
- [ ] 所有表单输入均有实时校验反馈
- [ ] 密码以BCrypt方式加密存储

***

## 三、模块二：商城首页

### 3.1 功能需求清单

| 编号   | 功能点           | 优先级 | 说明                            |
| ---- | ------------- | --- | ----------------------------- |
| H-01 | Hero Banner区  | P0  | 全屏沉浸式旗舰产品展示                   |
| H-02 | Bento Box商品网格 | P0  | 不规则网格展示热门商品                   |
| H-03 | 特色服务横幅        | P1  | 免费配送/正品保障/售后无忧/分期免息           |
| H-04 | 全局导航栏         | P0  | Logo + 导航链接 + 搜索 + 购物袋 + 用户入口 |
| H-05 | 移动端适配         | P0  | 响应式布局，移动端抽屉菜单                 |
| H-06 | 滚动动效          | P1  | Header毛玻璃效果 + 内容淡入动画          |

### 3.2 页面区块详解

#### 区块A: 全局导航栏（ClientHeader）

**位置**: 固定顶部，所有页面共享

**功能元素**:

| 元素        | 行为                | 说明                  |
| --------- | ----------------- | ------------------- |
| Logo      | 点击跳转首页            | 文字Logo "Lumina."    |
| 导航链接      | 首页 / 商品 / 关于 / 支持 | 当前页面高亮 + hover下划线动画 |
| 搜索图标      | 点击展开搜索框（二期实现搜索功能） | 一期为静态占位             |
| 购物袋图标     | 显示数量徽章（一期固定为0）    | 点击无反应（二期接入购物车）      |
| 用户头像/登录按钮 | 根据登录态切换           | 头像(已登录) / 登录按钮(未登录) |
| 移动端菜单图标   | 点击打开右侧抽屉          | 仅移动端显示              |

**交互特性**:

- 默认透明背景，滚动后变为 `bg-lumina-100/80 backdrop-blur-xl`
- 过渡动画时长 500ms
- 高度固定 56px (h-14)

#### 区块B: Hero Banner区

**视觉设计**（遵循[Lumina设计风格](./Lumina数码商城设计风格.md) §6.1.A）:

- **高度**: 85vh（最小600px）
- **背景**: 从 `#0a0a0a` 到 `#1a1a1c` 的垂直渐变
- **内容**:
  - 大标题: 产品名称（如 "iPhone 15 Pro"），字号响应式 text-5xl \~ text-8xl
  - 副标题: 产品Slogan，70%白色透明度
  - CTA按钮组: 主按钮（白底）+ 次按钮（描边）
  - 产品大图: 居中底部，带浮动动画（±10px，6秒周期）
- **动效**:
  - 标题/副标题/按钮依次淡入上移（staggered animation）
  - 产品图缓慢上下浮动
  - 底部滚动指示器（弹跳动画）

**数据来源**:

- 一期使用硬编码Mock数据（1个Hero产品）
- 后端预留 Banner API 接口（二期对接CMS）

#### 区块C: Bento Box商品网格

**布局结构**（遵循设计风格 §6.1.B）:

```
桌面端 (≥1024px): 4列网格，不规则排列
┌─────────────────────┬──────────┬──────────┐
│                     │          │          │
│   旗舰产品 (2x2)     │ 商品B    │ 商品C    │
│   col-span-2        │ (1x1)    │ (1x1)    │
│   row-span-2        │          │          │
├─────────────────────┼──────────┴──────────┤
│                     │   商品D (跨2列)      │
│                     │   col-span-2        │
└─────────────────────┴─────────────────────┘

移动端 (<768px): 单列堆叠
```

**商品卡片内容**:

| 元素     | 说明               | 数据来源                  |
| ------ | ---------------- | --------------------- |
| 分类标签   | 如 "智能手机"、"笔记本电脑" | product.categoryName  |
| 商品名称   | 最多2行截断           | product.name          |
| 商品图片   | 正方形比例，hover缩放    | product.mainImage     |
| 价格     | ¥格式化显示           | product.price         |
| 原价（可选） | 删除线显示            | product.originalPrice |
| 徽章（可选） | 如 "新品"、"热卖"      | product.badge         |

**交互效果**:

- 卡片悬停: 上移4px + 柔和阴影放大 + 图片scale(1.05)
- 点击: 跳转商品详情页 `/product/:id`
- 图片加载: lazy loading

**数据来源**:

- 一期: Mock数据（6-8个商品）
- API预留: `GET /api/v1/products/recommended` （二期实现）

#### 区块D: 特色服务横幅

**四宫格布局**:

| 图标  | 标题   | 描述文案        |
| --- | ---- | ----------- |
| 🚐  | 免费配送 | 订单满¥999即享包邮 |
| 🛡️ | 正品保障 | 官方授权渠道      |
| 🔧  | 售后无忧 | 7天无理由退换     |
| 💳  | 分期免息 | 3/6/12期免息可选 |

- 背景: 白色，上下边框
- 图标容器: 圆形浅灰背景，hover变深
- 响应式: 移动端2x2网格，桌面端1x4横排

#### 区块E: Footer页脚

**内容结构**（遵循设计风格 §5.2.C）:

- **背景色**: `bg-lumina-900`（深空灰）
- **链接列**: 产品 / 服务 / 关于 / 支持（各5-6个链接）
- **底部**: 版权声明 + 法律条款链接
- **文字颜色**: 主文字 lumina-200，次级 lumina-400/lumina-500

### 3.3 前端组件结构

```
frontend/src/
├── views/
│   └── HomeView.vue                    # 首页主视图
├── layouts/
│   └── ClientLayout.vue                # C端全局布局（Header + RouterView + Footer）
├── components/
│   ├── client/
│   │   ├── AppHeader.vue               # 顶部导航栏
│   │   ├── MobileMenuDrawer.vue        # 移动端侧边抽屉菜单
│   │   ├── AppFooter.vue               # 页脚
│   │   ├── HeroSection.vue             # Hero Banner区
│   │   ├── BentoGridSection.vue        # Bento Box商品网格
│   │   ├── ProductCard.vue             # 商品卡片（可复用）
│   │   └── FeatureStrip.vue            # 特色服务横幅
│   └── common/
│       └── ScrollIndicator.vue         # 滚动指示器组件
├── composables/
│   ├── useScroll.ts                    # 滚动位置监听
│   └── useScrollReveal.ts             # 滚动触发动画
├── stores/
│   └── user.ts                         # 用户状态Store
├── types/
│   └── product.ts                      # 商品类型定义
└── mock/
    └── home.ts                         # 首页Mock数据
```

### 3.4 Mock数据结构

**Hero Banner数据**:

```typescript
interface HeroData {
  title: string              // "iPhone 15 Pro"
  subtitle: string           // "钛金属设计。A17 Pro芯片。"
  description: string        // "一部开创先机的iPhone。"
  primaryCta: { text: string; link: string }   // { text: "购买", link: "/product/1" }
  secondaryCta: { text: string; link: string } // { text: "进一步了解", link: "/product/1" }
  image: string              // 产品图片URL
  bgColor: string            // 背景色（暗色系）
}
```

**Bento Grid商品数据**:

```typescript
interface ProductCardData {
  id: number
  name: string               // 商品名称
  categoryName: string       // 分类名称
  mainImage: string          // 主图URL
  price: number              // 现价
  originalPrice?: number     // 原价（可选）
  badge?: string             // 徽章（可选）
  gridSpan?: {               // 网格占据（可选，默认1x1）
    col: number              // 列数 1|2
    row: number              // 行数 1|2
  }
  bgVariant?: 'light' | 'dark' // 背景变体（默认light）
}
```

### 3.5 验收标准

- [ ] 导航栏固定顶部，滚动后出现毛玻璃效果
- [ ] 导航栏正确反映登录状态（头像/登录按钮切换）
- [ ] Hero区全屏沉浸式展示，标题/按钮有入场动画
- [ ] Hero区产品图有缓慢浮动效果
- [ ] Bento Grid在桌面端呈不规则网格布局
- [ ] Bento Grid在移动端单列堆叠
- [ ] 商品卡片悬停有上浮+阴影+图片缩放效果
- [ ] 点击商品卡片正确跳转到商品详情页
- [ ] 特色服务横幅四宫格正确展示
- [ ] Footer多列链接布局完整
- [ ] 移动端汉堡菜单点击弹出右侧抽屉
- [ ] 所有图片懒加载生效
- [ ] 页面滚动流畅，无明显卡顿

***

## 四、模块三：商品详情页

### 4.1 功能需求清单

| 编号   | 功能点    | 优先级 | 说明                   |
| ---- | ------ | --- | -------------------- |
| D-01 | 产品画廊   | P0  | 主图 + 缩略图切换           |
| D-02 | 基本信息   | P0  | 名称/副标题/价格/原价         |
| D-03 | SKU选择器 | P0  | 多维度规格选择（颜色/容量/版本等）   |
| D-04 | 数量选择器  | P0  | 加减按钮 + 数值输入          |
| D-05 | 吸顶购买栏  | P1  | 滚动后固定顶部的快捷购买条        |
| D-06 | 产品亮点介绍 | P1  | 三列图文展示核心卖点           |
| D-07 | 技术参数表  | P1  | Key-Value形式参数列表      |
| D-08 | 面包屑导航  | P0  | 首页 > 分类 > 商品名        |
| D-09 | 明暗交替区块 | P0  | 视觉节奏控制（Dark/Light交替） |

### 4.2 页面区块详解

#### 区块A: 产品外观展示区（暗色背景）

**背景色**: `bg-lumina-950`（近黑）

**左侧 — 产品画廊（占比约50%）**:

| 元素     | 规格                                   | 说明            |
| ------ | ------------------------------------ | ------------- |
| 主图容器   | aspect-square, rounded-3xl           | 正方形展示区，圆角24px |
| 主图     | max-w-full max-h-full object-contain | 保持比例，居中显示     |
| 缩略图列表  | 横向排列，间距12px                          | 64x64px 圆角缩略图 |
| 缩略图选中态 | border-white (2px边框)                 | 当前选中高亮        |
| 缩略图默认态 | border-transparent, opacity-60       | 半透明未选中        |
| 图片数量   | 3-5张                                 | 不同角度/场景的产品图   |

**右侧 — 产品信息 + 购买选项（占比约50%）**:

| 元素     | 说明                                           |
| ------ | -------------------------------------------- |
| 面包屑    | 首页 / 分类名 / 商品名称                              |
| 产品名称   | text-3xl\~4xl, font-semibold, tracking-tight |
| 副标题    | text-lg, text-white/60, 产品Slogan             |
| 价格     | text-3xl, font-bold, ¥格式化                    |
| 原价     | text-lg, line-through, text-white/40（如有折扣）   |
| SKU选择器 | 多组规格选项，每组一个label + 多个option按钮                |
| 数量选择器  | 圆形加减按钮 + 数字输入框，限制库存范围                        |
| CTA按钮组 | "加入购物袋"（主按钮）+ "立即购买"（次按钮）                    |

**SKU选择器详细规范**:

```typescript
interface SkuGroup {
  name: string              // 规格组名称，如 "颜色", "容量", "版本"
  options: SkuOption[]
}

interface SkuOption {
  value: string             // 规格值，如 "钛金属蓝", "256GB"
  label: string             // 显示文本（可与value相同）
  disabled?: boolean        // 是否禁用（缺货时）
  imageUrl?: string         // 规格对应的图片（如颜色预览图）
}
```

**SKU按钮样式**:

- 未选中: `border-white/20 text-white/80` → hover: `border-white/50 bg-white/5`
- 已选中: `border-white bg-white text-lumina-950`（反色高亮）
- 禁用: `opacity-30 cursor-not-allowed`
- 形状: `rounded-full`（胶囊形），padding: `px-5 py-2.5`

**数量选择器规范**:

- 最小值: 1
- 最大值: 当前SKU库存量
- 外观: 圆形边框容器 `border border-white/20 rounded-full`
- 减号按钮: 禁用时 opacity 降低
- 输入框: 隐藏原生spinner，居中显示数字

#### 区块B: 产品亮点介绍（亮色背景）

**背景色**: `bg-lumina-100`（浅灰）

**布局**: 3列等宽网格（移动端堆叠）

每个亮点项包含:

- 图标容器: 64x64px, rounded-2xl, 浅灰背景
- 图标: 28px, lumina-600色
- 标题: text-lg, font-semibold, lumina-800
- 描述: text-sm, lumina-500, leading-relaxed

**示例亮点**（以手机为例）:

| 图标           | 标题    | 描述               |
| ------------ | ----- | ---------------- |
| ⚡️ A17 Pro芯片 | 性能飞跃  | 下一代GPU架构，主机级游戏体验 |
| 📸 Pro相机系统   | 影像革命  | 4800万像素主摄，4倍光学变焦 |
| 🔋 续航升级      | 全天候陪伴 | 视频播放最长29小时       |

#### 区块C: 技术参数表（深灰背景）

**背景色**: `bg-lumina-900`（深空灰）

**布局**: 居中容器，最大宽度 `max-w-4xl`

**表格样式**:

- 分割线: `divide-y divide-white/10`（极弱白色分割）
- 左侧标签: `text-sm text-white/60`（60%白色）
- 右侧值: `text-sm font-medium text-right`（右对齐）
- 行高: `py-4`（垂直 padding 16px）

**示例参数**（以手机为例）:

| 参数    | 值                              |
| ----- | ------------------------------ |
| 显示屏   | 6.7英寸 Super Retina XDR显示屏      |
| 芯片    | A17 Pro芯片                      |
| 存储    | 128GB / 256GB / 512GB / 1TB    |
| 后置摄像头 | 4800万像素主摄 + 1200万超广角 + 1200万长焦 |
| 前置摄像头 | 1200万像素 TrueDepth              |
| 电池    | 视频播放最长29小时                     |
| 充电    | USB-C, 支持快充和MagSafe无线充电        |
| 尺寸    | 159.9 x 76.7 x 8.25 mm         |
| 重量    | 221克                           |
| 防水    | IP68                           |

#### 区块D: 吸顶购买栏（Sticky Buy Bar）

**触发条件**: 滚动距离超过首屏产品图底部

**外观**:

- 定位: `fixed top-14 left-0 right-0 z-40`（紧贴Header下方）
- 高度: `h-14`（56px）
- 背景: `bg-white/95 backdrop-blur-lg`（95%不透明 + 强模糊）
- 底部边框: `border-b border-lumina-200 shadow-sm`

**内容**:

- 左侧: 产品名称（truncate, max-width限制）
- 右侧: 价格 + 购买按钮（rounded-full）

**动画**: 从顶部滑入，300ms弹性缓动（slide-down transition）

### 4.3 数据结构定义

**ProductDetailVO（商品详情响应）**:

```typescript
interface ProductDetailVO {
  id: number
  name: string                 // 商品名称
  shortName: string            // 简称（用于标题引用）
  subtitle: string             // 副标题/Slogan
  categoryName: string         // 所属分类
  mainImage: string            // 主图
  images: string[]             // 图片列表（含主图）
  price: number                // 现价
  originalPrice?: number       // 原价
  stock: number                // 总库存
  skuGroups: SkuGroup[]        // SKU规格组
  features: FeatureItem[]      // 产品亮点
  specifications: SpecItem[]   // 技术参数
}

interface FeatureItem {
  icon: string                 // 图标组件名（Element Plus Icon）
  title: string                // 亮点标题
  description: string          // 亮点描述
}

interface SpecItem {
  label: string                // 参数名
  value: string                // 参数值
}
```

### 4.4 前端组件结构

```
frontend/src/views/
└── ProductDetailView.vue              # 商品详情页主视图

frontend/src/components/client/detail/
├── ProductGallery.vue                 # 产品画廊（主图+缩略图）
├── ProductInfoPanel.vue               # 产品信息面板（名称+价格+SKU+数量+CTA）
├── SkuSelector.vue                    # SKU选择器（可复用组件）
├── QuantitySelector.vue               # 数量选择器（可复用组件）
├── StickyBuyBar.vue                   # 吸顶购买栏
├── FeatureSection.vue                 # 产品亮点介绍区
├── SpecSection.vue                    # 技术参数表区
└── BreadcrumbNav.vue                  # 面包屑导航
```

### 4.5 Mock数据示例

一期使用硬编码的Mock数据，建议准备至少 **1个完整商品详情数据**（如 iPhone 15 Pro 或 MacBook Pro），包含:

- 4-5张产品图片
- 2-3组SKU规格（如: 颜色\[钛金属蓝/自然钛/白色] + 容量\[256GB/512GB/1TB]）
- 3个产品亮点
- 10-15条技术参数

### 4.6 页面交互流程

```
用户进入详情页
    ↓
加载Mock数据 → 渲染页面
    ↓
用户浏览产品图 → 点击缩略图切换主图
    ↓
用户选择SKU → 更新选中状态 + 校验SKU组合有效性
    ↓
用户调整数量 → 限制在1~库存范围内
    ↓
用户向下滚动 → 超过首屏 → 吸顶购买栏滑入
    ↓
继续滚动 → 亮点区(亮色) → 参数区(深灰) → 明暗交替节奏
    ↓
点击"加入购物袋"/"立即购买" → 一期Toast提示（二期接入购物车/结算）
```

### 4.7 验收标准

- [ ] 面包屑导航正确显示层级关系
- [ ] 产品画廊主图与缩略图联动切换
- [ ] 缩略图选中态视觉反馈清晰
- [ ] 产品名称/价格/副标题正确渲染
- [ ] SKU选择器每组只能单选
- [ ] SKU选中状态视觉反色高亮
- [ ] 数量选择器加减正常工作，边界值处理正确
- [ ] 滚动超过首屏后吸顶购买栏出现
- [ ] 吸顶购买栏显示正确的商品名和价格
- [ ] 产品亮点区三列布局正确（桌面端）
- [ ] 技术参数表Key-Value对齐美观
- [ ] 明暗背景交替产生视觉节奏感
- [ ] 移动端单列布局，图片在上信息在下
- [ ] 页面滚动流畅，无布局抖动

***

## 五、公共基础设施

### 5.1 后端依赖添加（pom.xml新增）

```xml
<!-- MyBatis-Plus -->
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
    <version>3.5.5</version>
</dependency>

<!-- MySQL驱动 -->
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <scope>runtime</scope>
</dependency>

<!-- JWT -->
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.3</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.3</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.3</version>
    <scope>runtime</scope>
</dependency>

<!-- Spring Security（用于密码加密，暂不启用完整安全链） -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>

<!-- Lombok -->
<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <optional>true</optional>
</dependency>

<!-- Redis（可选，用于Token黑名单/缓存） -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
```

### 5.2 前端依赖添加

```bash
# UI组件库
npm install element-plus @element-plus/icons-vue

# CSS框架
npm install tailwindcss @tailwindcss/vite

# HTTP客户端
npm install axios

# 工具库
npm install @vueuse/core
```

### 5.3 项目目录结构（最终形态）

```
GoBuy/
├── backend/                          # Spring Boot后端
│   ├── src/main/java/com/example/backend/
│   │   ├── config/                   # 配置类
│   │   ├── controller/               # 控制器
│   │   ├── service/                  # 业务层
│   │   ├── entity/                   # 实体类
│   │   ├── mapper/                   # Mapper
│   │   ├── dto/                      # 数据传输对象
│   │   ├── common/                   # 公共工具
│   │   └── util/                     # 工具类
│   └── src/main/resources/
│       ├── application.yml           # 应用配置
│       └── mapper/                   # XML映射文件（如需）
│
├── frontend/                         # Vue 3前端
│   ├── src/
│   │   ├── api/                      # API请求封装
│   │   ├── assets/                   # 静态资源（图片/字体）
│   │   ├── components/              # 组件
│   │   │   ├── client/              # C端业务组件
│   │   │   └── common/              # 公共组件
│   │   ├── composables/             # 组合式函数
│   │   ├── layouts/                 # 布局组件
│   │   ├── mock/                    # Mock数据
│   │   ├── router/                  # 路由配置
│   │   ├── stores/                  # Pinia状态管理
│   │   ├── styles/                  # 全局样式
│   │   ├── types/                   # TypeScript类型
│   │   ├── utils/                   # 工具函数
│   │   ├── views/                   # 页面视图
│   │   ├── App.vue
│   │   └── main.ts
│   └── public/                      # 公共资源
│
├── 一期工程.md                        # 本文档
└── Lumina数码商城设计风格.md           # 设计规范文档
```

### 5.4 配置文件模板

**backend/src/main/resources/application.yml**:

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gobuy?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      id-type: auto

jwt:
  secret: your-256-bit-secret-key-at-least-32-characters
  access-token-expiration: 7200000    # 2小时（毫秒）
  refresh-token-expiration: 604800000 # 7天（毫秒）
```

***

## 六、开发排期建议

### 阶段划分

| 阶段          | 内容         | 预估工作量  | 产出物                                     |
| ----------- | ---------- | ------ | --------------------------------------- |
| **Phase 0** | 项目初始化与环境搭建 | 0.5天   | 依赖安装、目录创建、基础配置                          |
| **Phase 1** | 后端用户模块开发   | 1.5天   | 注册/登录API、JWT工具、Security配置               |
| **Phase 2** | 前端登录注册页面   | 1天     | LoginView、UserStore、路由守卫、Axios封装        |
| **Phase 3** | 前端布局与首页    | 2天     | ClientLayout、Header/Footer、HomeView全部区块 |
| **Phase 4** | 前端商品详情页    | 2天     | ProductDetailView及其子组件                  |
| **Phase 5** | 联调与优化      | 1天     | 前后端联调、响应式测试、性能优化                        |
| **合计**      | <br />     | **8天** | <br />                                  |

### 开发顺序建议

```
Day 1-2:  后端基础搭建 + 用户模块
  ↓
Day 3:    前端环境配置 + 登录注册页 + 状态管理
  ↓
Day 4-5:  全局布局（Header/Footer）+ 首页Hero + Bento Grid
  ↓
Day 6-7:  商品详情页（画廊 + 信息面板 + SKU + 参数）
  ↓
Day 8:    联调优化 + Bug修复 + 验收检查
```

***

## 七、质量要求

### 7.1 代码规范

- 遵循用户自定义规则：SRP、DRY、Fail-Fast
- RESTful API命名规范（小写、中划线、名词）
- DTO/VO分层，禁止Entity直接暴露
- TypeScript严格模式，避免any类型
- Vue组件使用 `<script setup>` + Composition API

### 7.2 设计还原度

- 严格按照 [Lumina数码商城设计风格.md](./Lumina数码商城设计风格.md) 执行
- 色彩、圆角、阴影、字体必须使用设计变量
- 动效时长和缓动曲线符合规范
- 响应式断点和布局调整符合规范

### 7.3 性能指标

| 指标          | 目标值            |
| ----------- | -------------- |
| 首页FCP       | < 1.5s         |
| 首页LCP       | < 2.5s         |
| 详情页FCP      | < 1.2s         |
| JS bundle大小 | < 500KB (gzip) |
| 图片懒加载       | 100%非首屏图片      |

### 7.4 兼容性要求

| 平台             | 最低版本 |
| -------------- | ---- |
| Chrome         | ≥ 90 |
| Safari         | ≥ 14 |
| Firefox        | ≥ 88 |
| Edge           | ≥ 90 |
| iOS Safari     | ≥ 14 |
| Android Chrome | ≥ 90 |

***

## 八、风险与注意事项

| 风险项                  | 影响      | 应对策略                            |
| -------------------- | ------- | ------------------------------- |
| Element Plus主题定制复杂度高 | UI还原度不足 | 优先覆盖CSS变量，深度定制按需进行              |
| GSAP学习成本             | 动效实现延期  | 一期先用CSS动画，GSAP后续迭代引入            |
| Mock数据真实性差           | 页面空洞    | 使用真实产品文案和合理价格数据                 |
| 移动端适配遗漏              | 移动体验差   | 每个组件完成后立即测试移动端                  |
| JWT安全性               | Token泄露 | 生产环境务必使用HTTPS + HttpOnly Cookie |

***

> **文档维护**: 本文档为一期的唯一权威规划，开发过程中如需变更须经确认后更新本文档。
> **下一步**: 确认本规划后，按照第六章的开发排期依次执行各阶段任务。

